@model Dictionary<DateTime, IEnumerable<IEvent>>

@if (Model.Count() > 0)
{
    @foreach (var key in Model.Keys)
    {
        <h2>@key.ToString("MMMM yyyy")</h2>
        @await Html.PartialAsync("ListEvents", Model[key]);
    }
}
else
{
    <div>
        Записей нет.
    </div>
}